{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Uruchom serwer",
            "type": "shell",
            "options": {    
                "cwd": "${workspaceFolder}\\backend"
            },
            "command": "npm start",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}